// indexOf method returns the index number of specified value.
// if value not exist then return -1
// second param indicate the index number from where search start. by default it start from 0 index

const fruits = ['Banana','Orange','Apple','Mango']
const index = fruits.indexOf('Orange',1)
console.log(index)